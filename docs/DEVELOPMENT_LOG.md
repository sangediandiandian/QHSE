# QHSE 自动化开发记录

## 2026-07-15 · Stage 3A 隐患排查治理后端化

计划提交：`build hazard management workflow`

完成内容：

- 新增隐患、整改证据、业务操作轨迹三类 Prisma 模型和数据库种子。
- 新增内存/Prisma 双仓储、查询、上报、开始整改、证据归档、提交验收、验收关闭和挂牌督办接口。
- 服务端强制执行四态状态机、证据门槛、日期规则、风险区域派生和乐观并发控制。
- 新增 `hazard:read/report/rectify/accept/supervise` 权限，并完成 8 类角色和区域数据范围配置。
- 前端增加显式 Mock/API 数据模式、隐患 API 服务、异步状态更新、菜单/按钮权限；API 模式不再使用共享 dashboard 隐患缓存。
- 风险种子扩充为 6 个风险单元，保证隐患上报关联对象完整。

验证记录：

- Prisma schema validate：通过。
- TypeScript：前后端类型检查通过。
- Jest：隐患服务、风险服务和前端隐患工作流共 16 项测试通过。
- 前端生产构建：通过。
- 运行时联调：全厂 6 条、区域授权 2 条、缺权限 403、上报至验收关闭版本 1→5 全流程通过。
- ESLint：本阶段文件无新增错误；全局检查仍有 11 个既有错误，位于未改动的 `BigScreen` 和 `Communication` 页面，留待对应阶段处理。

## 2026-07-15 · Stage 3B 作业许可管理后端化

计划提交：`build work permit approval workflow`

完成内容：

- 新增作业票、三级审批节点、现场确认三类 Prisma 模型及 5 张业务种子票证。
- 新增内存/Prisma 双仓储和申请、查询、逐级审批、双人确认、暂停建议、暂停、复测恢复、关闭接口。
- 服务端按属地、QHSE、负责人三类角色校验审批节点，并禁止同一账号完成两个现场确认角色。
- 新增 `permit:read/apply/approve/confirm/control` 权限点，完成 8 类角色和区域数据范围配置。
- 前端新增独立作业许可 API 数据切片、异步交互和菜单/按钮权限；申请人、签署人、确认人不再由客户端伪造。
- API 模式明确提示实时告警自动判定依赖下一阶段规则执行引擎，当前保留可审计的暂停建议领域接口。

验证记录：

- Prisma schema validate：通过。
- TypeScript：前后端类型检查通过。
- Jest：后端作业许可服务与前端工作流共 10 项测试通过。
- 前端生产构建：通过。
- 运行时联调：区域过滤、缺权限 403、三账号逐级审批、双账号现场确认、暂停建议至关闭版本 1→10 全流程通过。

## 2026-07-15 · Stage 4A 通用审批流内核

计划提交：`build reusable approval workflow core`

完成内容：

- 新增通用审批实例和审批节点 Prisma 模型，以及内存/Prisma 双仓储。
- 支持按业务类型和业务 ID 关联流程、有序节点、节点角色白名单、审批通过、驳回和发起人撤回。
- 防止同一业务重复创建活动流程，末节点通过后自动完成，驳回或撤回后禁止继续处理。
- 所有动作使用 `expectedVersion` 防止并发覆盖，服务导出给预警规则、预案和事件关闭等领域复用。
- 未暴露绕过领域权限的通用审批控制器，后续由各业务服务进行权限与审计封装。

验证记录：

- Prisma Client 生成和后端 TypeScript 构建：通过。
- Jest：流程创建防重、分权审批、版本冲突、驳回和撤回共 5 项测试通过。

## 2026-07-15 · Stage 4B 预警规则配置与发布后端化

计划提交：`build warning rule publishing workflow`

完成内容：

- 新增预警规则和不可变规则版本 Prisma 模型、6 条已发布种子规则及内存/Prisma 双仓储。
- 新增规则查询、草稿创建/维护、提交、审批、驳回、回滚为草稿和显式启停接口。
- 草稿提交时由服务端检测启用规则的作用域和表达式重复，冲突规则返回稳定业务错误及编码列表。
- 复用通用审批流完成 QHSE/生产负责人双会签，并补充同一账号不能连续完成多个会签节点的双人控制。
- 新增 `warning:read/edit/submit/approve/toggle` 权限点，完成 8 类角色分配和写操作审计。
- 前端新增独立规则 API 数据切片、异步交互、菜单/按钮权限；API 模式不再使用 dashboard 规则快照。

验证记录：

- Prisma schema validate、前后端 TypeScript、阶段文件 ESLint：通过。
- Jest：规则领域、通用审批流和前端原型工作流共 16 项测试通过。
- 前端生产构建：通过。
- 运行时联调：6 条种子查询、无权限 403、草稿、双账号会签发布、启停和回滚，规则修订 1→6 全流程通过。

## 2026-07-15 · Stage 4C 预警规则实时执行

计划提交：`build warning rule execution engine`

完成内容：

- 新增规则执行状态和预警信号 Prisma 模型，以及内存/Prisma 双仓储。
- 新增统一采样执行与信号查询接口，支持 GDS、VOC、MES 和联合预警标准样本。
- 执行器支持 AND/OR 表达式、持续时长、联合数据时间窗口和按对象确定性灰度分群。
- 同一规则与对象 5 分钟内抑制重复有效信号；触发次数和最近触发时间独立更新，不污染规则配置修订号。
- 较大/重大信号命中后，按已发布的作业许可联动规则自动为同区域作业票生成暂停建议。
- 新增 `warning:evaluate` 权限；前端规则页面增加测试样本执行、触发统计刷新和近期信号展示。

验证记录：

- Prisma schema validate、前后端 TypeScript、阶段文件 ESLint：通过。
- Jest：规则执行器、规则发布、通用审批流和前端规则工作流共 21 项测试通过。
- 前端生产构建：通过。
- 运行时联调：操作员执行被拒绝 403，GDS 样本触发 `rule-001`，5 分钟内重复样本被抑制，生成 1 条信号，并将 `permit-001` 更新为“建议暂停”。

## 2026-07-15 · Stage 5A 应急事件生命周期后端化

计划提交：`build emergency event lifecycle`

完成内容：

- 新增应急事件 Prisma 模型、4 条种子事件及内存/Prisma 双仓储。
- 新增告警转事件、查询、研判启动、响应升级/降级、终止监控、证据归档、关闭申请、催办和审批接口。
- 服务端拒绝同一告警重复转事件，状态机拒绝非法迁移，所有写操作使用版本号防止并发覆盖。
- 关闭申请复用通用审批流生成 4 小时 QHSE 审批任务，并强制申请人与审批人异人、关闭前至少存在一项证据。
- 新增 `emergency:read/manage/evidence/approve` 权限、区域数据范围和写操作审计；证据上传人、操作人和签署人均取认证主体。
- 前端事件闭环页面接入独立 API 数据切片、异步状态更新、菜单/按钮权限和服务端持久化提示。

验证记录：

- Prisma schema validate、前后端 TypeScript、阶段文件 ESLint：通过。
- Jest：应急事件服务、通用审批流和前端原型工作流共 17 项测试通过。
- 前端生产构建：通过。
- 运行时联调：4 条种子事件查询、操作员迁移 403、区域越权 404、研判启动、关闭申请、催办、QHSE 异人签署关闭及重复告警 409 均符合预期。

## 2026-07-15 · Stage 5B1 应急预案与演练后端化

计划提交：`build emergency plan workflow`

完成内容：

- 新增应急预案 Prisma 模型、3 套种子预案及内存/Prisma 双仓储。
- 新增预案查询、草稿创建/维护、提交评审、会签发布和历史版本回滚接口。
- 复用通用审批流执行 QHSE/生产负责人异人双会签，发布生成不可变版本，回滚只生成独立草稿。
- 新增演练计划、开始演练和评分复盘接口，全部使用预案修订号进行乐观并发控制。
- 新增 `plan:read/edit/submit/approve/drill` 权限和写操作审计。
- 前端预案库接入独立 API 数据切片、异步更新、菜单/按钮权限，演练全流程改由服务端持久化。

验证记录：

- Prisma schema validate、前后端 TypeScript、前端阶段文件 ESLint：通过。
- Jest：预案服务、通用审批流和前端原型工作流共 17 项测试通过。
- 前端生产构建：通过。
- 运行时联调：3 套种子查询、无权限访问 403、历史回滚、双账号会签发布 `V3.3`、演练计划→待复盘→92 分完成全流程通过。

## 2026-07-15 · Stage 5B2 应急资源后端化

计划提交：`build emergency resource inventory`

完成内容：

- 新增应急资源 Prisma 模型、3 类种子资源及内存/Prisma 双仓储。
- 新增资源入库、批次补充、调拨、到位、归还和巡检接口，写操作统一使用修订号进行乐观并发控制。
- 调拨按有效期 FEFO 自动分配批次，排除过期库存并阻断维护中资源；归还同步恢复批次和汇总可用库存。
- 新增 `resource:read/manage/dispatch/inspect` 权限、可信操作人和统一写操作审计。
- 前端资源管理接入独立 API 数据切片、异步刷新及菜单/按钮权限，API 模式不再依赖驾驶舱资源缓存。

验证记录：

- Prisma schema validate、前后端 TypeScript、前端阶段文件 ESLint：通过。
- Jest：应急资源服务和前端资源工作流共 13 项测试通过。
- 前端生产构建和后端生产编译：通过。
- 运行时联调：3 类种子查询、操作员调拨 403、呼吸器按近效期批次 `4 + 1` 分配、到位、归还恢复 12 套库存、维护资源调拨 409、可信巡检人均符合预期。

## 2026-07-15 · Stage 5C 融合通信后端化

计划提交：`build communication delivery workflow`

完成内容：

- 新增通信事件 Prisma 模型、3 条种子事件及内存/Prisma 双仓储。
- 新增通信事件查询、逐级升级、送达/失败回执和人员确认接口。
- 服务端固化 0/2/3/5 分钟重呼、班长、负责人/调度升级链，确认后禁止继续升级。
- 失败回执自动生成重试任务并限制最多重试 2 次；确认人来自认证主体。
- 新增 `communication:read/send/confirm` 权限、统一写操作审计和乐观并发控制。
- 前端融合通信控制台接入独立 API 数据切片、异步更新、菜单/按钮权限，并修复原有数组图标缺少 `key` 的 ESLint 问题。

验证记录：

- Prisma schema validate、前后端 TypeScript、前端阶段文件 ESLint：通过。
- Jest：通信服务和前端原型升级工作流共 11 项测试通过。
- 前端生产构建和后端生产编译：通过。
- 运行时联调：3 条种子查询、操作员升级 403、电话重呼、失败回执生成第 2 次重试、送达回执、可信确认人 `王强`、确认后继续升级 409 均符合预期。

## 2026-07-15 · Stage 6A1 统一遥测后端接入

计划提交：`build telemetry ingestion pipeline`

完成内容：

- 新增遥测点位和样本 Prisma 模型、6 个 GDS/VOC/MES 种子点位及内存/Prisma 双仓储。
- 新增点位筛选、详情、历史样本和标准样本写入接口。
- 样本使用外部 `sampleId` 幂等落库，质量码驱动在线/故障状态，并按各数据源点位阈值派生报警状态。
- 有效样本自动进入预警规则执行器，复用表达式、窗口、抑制和作业票联动能力；重复样本不重复触发规则。
- 新增 `telemetry:read/ingest` 权限、区域数据范围和写入审计。

验证记录：

- Prisma schema validate、后端 TypeScript 和差异检查：通过。
- Jest：遥测服务和预警执行服务共 9 项测试通过。
- 运行时联调：操作员仅查询所属区域 2 个点位、跨区域查询 404、写入 403；调度写入 GDS 二级样本后点位升至 `level2` 并评估 3 条规则，重复写入保持版本 2 且历史仅 1 条。

## 2026-07-15 · Stage 6A2 监测页面 API 接入

计划提交：`connect monitoring pages to telemetry api`

完成内容：

- 新增前端遥测通用类型、API 服务及 GDS/VOC/MES 视图模型映射。
- 模型新增三个监测数据切片、统一加载状态和样本写入后的局部刷新。
- GDS、VOC、MES 页面脱离共享 dashboard 数据，在 API 模式直接读取服务端点位并保留 Mock 演示模式。
- 三个模拟入口在 API 模式写入真实标准样本并执行服务端预警规则，按 `telemetry:ingest` 控制按钮权限。
- 显式向 Umi 浏览器包注入 `REACT_APP_QHSE_DATA_MODE`，修复此前 API 模式环境变量未生效的问题。

验证记录：

- 前端 TypeScript、阶段文件 ESLint、7 项遥测前后端测试和 API 模式生产构建：通过。
- 浏览器联调：GDS 18→42 `%LEL` 并进入二级报警；VOC 56→66 `mg/m³` 并显示超限/效率下降；MES 0.82→1.35 MPa 并显示参数异常、GDS 二级证据和联合规则已触发；无运行时 `TypeError`。

## 2026-07-15 · Stage 6A3 MQTT/WebSocket 实时遥测传输

计划提交：`build realtime telemetry transport`

完成内容：

- 新增可选 MQTT QoS 1 适配器，解析 `qhse/telemetry/{source}/{pointId}` 标准主题并复用统一幂等、状态派生和预警执行服务；未配置 broker 时保持禁用且不影响启动。
- 新增 Socket.IO `/telemetry` 实时通道，握手校验现有 Bearer 会话与 `telemetry:read` 权限，并按角色区域范围及订阅数据源过滤事件。
- 使用服务端单调序号和有界事件缓冲支持 `afterSequence` 断线补传，前端自动重连、保存游标，并在服务端重启序号归零时自动重置旧游标。
- 新增乱序样本策略：样本仍进入历史，但不覆盖点位当前值、不重复执行规则；未来时钟偏差超过默认 60 秒时返回稳定错误。
- 新增集成状态接口，公开 MQTT 启用/连接状态、收发计数以及 WebSocket 最新/最早补传序号，不暴露 broker 密钥。
- GDS、VOC、MES 页面接入实时订阅并显示连接状态，外部样本到达后无需刷新即可更新点位。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：统一遥测、MQTT 解析、序号补传和前端游标/映射共 14 项测试通过。
- 运行时联调：岗位操作员仅收到所属区域 GDS 事件、跨区域 MES 被过滤；断线后从序号 2 补传到序号 3，集成状态接口返回一致序号。
- 浏览器联调：GDS 页面显示“实时通道：已连接”，外部 API 将 GDS-101 从 33 写为 29 后页面无需刷新即完成更新；未发现 WebSocket 运行错误。

## 2026-07-15 · Stage 6B 安全附件与证据存储

计划提交：`build secure evidence storage`

完成内容：

- 新增统一附件领域模型、内存/Prisma 双仓储及本地文件/S3 兼容对象存储适配器。
- 上传执行 20 MB 默认上限、MIME 白名单、区域数据范围校验和 SHA-256 计算，兼容 multipart 中文文件名。
- 对象元数据与二进制内容分离；对象只允许绑定一条业务记录，同一绑定幂等，避免跨业务复用证据。
- 新增附件上传、元数据查询和鉴权下载接口，二进制响应绕过统一 JSON 包装并设置安全下载头。
- 隐患整改和应急事件证据接入真实对象绑定，证据记录固化对象 ID、类型、大小和可信哈希。
- 新增 `attachment:read/upload` 权限，前端 API 模式支持文件选择、上传、大小/哈希展示和鉴权下载。

验证记录：

- Prisma schema validate、前后端 TypeScript、后端生产编译及差异检查：通过。
- Jest：附件、隐患和应急事件领域共 15 项测试通过。
- 运行时联调：岗位操作员跨区域上传返回 403；所属区域 DOCX 上传、隐患绑定和下载成功，上传/下载/源文件 SHA-256 完全一致。

## 2026-07-15 · Stage 7A 统计报表中心

计划提交：`build qhse reporting center`

完成内容：

- 新增报表领域模块，复用隐患、预警、作业许可和应急事件服务统一计算周期 KPI，不复制业务数据。
- 新增日期范围、区域筛选、逐日趋势和区域绩效明细，统计周期限制为最多 366 天。
- 报表层对四类数据执行统一的区域数据范围二次裁剪，避免无区域参数的信号仓储泄露跨区域计数。
- 定义区域风险指数口径并明确仅用于报表排序，不替代风险分级结果。
- 新增 UTF-8 BOM CSV 导出、字段转义和导出审计，以及 `report:read/export` 分权权限。
- 新增统计报表前端页面，支持日期/区域筛选、四类指标卡、近 30 天组合趋势、区域排行和 CSV 下载。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：报表聚合、区域隔离、日期/数据量边界和 CSV 转义共 5 项测试通过。
- 运行时联调：企业领导可见 5 个有数据区域，装置负责人仅见 `area-02`；岗位操作员读取返回 403，装置负责人跨区域筛选返回 404；CSV 返回 200 且 BOM 为 `EF BB BF`。
