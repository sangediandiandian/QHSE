# QHSE 自动化开发记录

## 2026-07-15 · Stage 3A 隐患排查治理后端化

计划提交：`build hazard management workflow`

完成内容：

- 新增隐患、整改证据、业务操作轨迹三类 Prisma 模型和数据库种子。
- 新增内存/Prisma 双仓储、查询、上报、开始整改、证据归档、提交验收、验收关闭和挂牌督办接口。
- 服务端强制执行四态状态机、证据门槛、日期规则、风险区域派生和乐观并发控制。
- 新增 `hazard:read/report/rectify/accept/supervise` 权限，并完成 8 类角色和区域数据范围配置。
- 前端增加显式 Mock/API 数据模式、隐患 API 服务、异步状态更新、菜单/按钮权限；API 模式不再使用共享 dashboard 隐患缓存。
- 风险种子扩充为 6 个风险单元，保证隐患上报关联对象完整。

验证记录：

- Prisma schema validate：通过。
- TypeScript：前后端类型检查通过。
- Jest：隐患服务、风险服务和前端隐患工作流共 16 项测试通过。
- 前端生产构建：通过。
- 运行时联调：全厂 6 条、区域授权 2 条、缺权限 403、上报至验收关闭版本 1→5 全流程通过。
- ESLint：本阶段文件无新增错误；全局检查仍有 11 个既有错误，位于未改动的 `BigScreen` 和 `Communication` 页面，留待对应阶段处理。

## 2026-07-15 · Stage 3B 作业许可管理后端化

计划提交：`build work permit approval workflow`

完成内容：

- 新增作业票、三级审批节点、现场确认三类 Prisma 模型及 5 张业务种子票证。
- 新增内存/Prisma 双仓储和申请、查询、逐级审批、双人确认、暂停建议、暂停、复测恢复、关闭接口。
- 服务端按属地、QHSE、负责人三类角色校验审批节点，并禁止同一账号完成两个现场确认角色。
- 新增 `permit:read/apply/approve/confirm/control` 权限点，完成 8 类角色和区域数据范围配置。
- 前端新增独立作业许可 API 数据切片、异步交互和菜单/按钮权限；申请人、签署人、确认人不再由客户端伪造。
- API 模式明确提示实时告警自动判定依赖下一阶段规则执行引擎，当前保留可审计的暂停建议领域接口。

验证记录：

- Prisma schema validate：通过。
- TypeScript：前后端类型检查通过。
- Jest：后端作业许可服务与前端工作流共 10 项测试通过。
- 前端生产构建：通过。
- 运行时联调：区域过滤、缺权限 403、三账号逐级审批、双账号现场确认、暂停建议至关闭版本 1→10 全流程通过。

## 2026-07-15 · Stage 4A 通用审批流内核

计划提交：`build reusable approval workflow core`

完成内容：

- 新增通用审批实例和审批节点 Prisma 模型，以及内存/Prisma 双仓储。
- 支持按业务类型和业务 ID 关联流程、有序节点、节点角色白名单、审批通过、驳回和发起人撤回。
- 防止同一业务重复创建活动流程，末节点通过后自动完成，驳回或撤回后禁止继续处理。
- 所有动作使用 `expectedVersion` 防止并发覆盖，服务导出给预警规则、预案和事件关闭等领域复用。
- 未暴露绕过领域权限的通用审批控制器，后续由各业务服务进行权限与审计封装。

验证记录：

- Prisma Client 生成和后端 TypeScript 构建：通过。
- Jest：流程创建防重、分权审批、版本冲突、驳回和撤回共 5 项测试通过。

## 2026-07-15 · Stage 4B 预警规则配置与发布后端化

计划提交：`build warning rule publishing workflow`

完成内容：

- 新增预警规则和不可变规则版本 Prisma 模型、6 条已发布种子规则及内存/Prisma 双仓储。
- 新增规则查询、草稿创建/维护、提交、审批、驳回、回滚为草稿和显式启停接口。
- 草稿提交时由服务端检测启用规则的作用域和表达式重复，冲突规则返回稳定业务错误及编码列表。
- 复用通用审批流完成 QHSE/生产负责人双会签，并补充同一账号不能连续完成多个会签节点的双人控制。
- 新增 `warning:read/edit/submit/approve/toggle` 权限点，完成 8 类角色分配和写操作审计。
- 前端新增独立规则 API 数据切片、异步交互、菜单/按钮权限；API 模式不再使用 dashboard 规则快照。

验证记录：

- Prisma schema validate、前后端 TypeScript、阶段文件 ESLint：通过。
- Jest：规则领域、通用审批流和前端原型工作流共 16 项测试通过。
- 前端生产构建：通过。
- 运行时联调：6 条种子查询、无权限 403、草稿、双账号会签发布、启停和回滚，规则修订 1→6 全流程通过。

## 2026-07-15 · Stage 4C 预警规则实时执行

计划提交：`build warning rule execution engine`

完成内容：

- 新增规则执行状态和预警信号 Prisma 模型，以及内存/Prisma 双仓储。
- 新增统一采样执行与信号查询接口，支持 GDS、VOC、MES 和联合预警标准样本。
- 执行器支持 AND/OR 表达式、持续时长、联合数据时间窗口和按对象确定性灰度分群。
- 同一规则与对象 5 分钟内抑制重复有效信号；触发次数和最近触发时间独立更新，不污染规则配置修订号。
- 较大/重大信号命中后，按已发布的作业许可联动规则自动为同区域作业票生成暂停建议。
- 新增 `warning:evaluate` 权限；前端规则页面增加测试样本执行、触发统计刷新和近期信号展示。

验证记录：

- Prisma schema validate、前后端 TypeScript、阶段文件 ESLint：通过。
- Jest：规则执行器、规则发布、通用审批流和前端规则工作流共 21 项测试通过。
- 前端生产构建：通过。
- 运行时联调：操作员执行被拒绝 403，GDS 样本触发 `rule-001`，5 分钟内重复样本被抑制，生成 1 条信号，并将 `permit-001` 更新为“建议暂停”。

## 2026-07-15 · Stage 5A 应急事件生命周期后端化

计划提交：`build emergency event lifecycle`

完成内容：

- 新增应急事件 Prisma 模型、4 条种子事件及内存/Prisma 双仓储。
- 新增告警转事件、查询、研判启动、响应升级/降级、终止监控、证据归档、关闭申请、催办和审批接口。
- 服务端拒绝同一告警重复转事件，状态机拒绝非法迁移，所有写操作使用版本号防止并发覆盖。
- 关闭申请复用通用审批流生成 4 小时 QHSE 审批任务，并强制申请人与审批人异人、关闭前至少存在一项证据。
- 新增 `emergency:read/manage/evidence/approve` 权限、区域数据范围和写操作审计；证据上传人、操作人和签署人均取认证主体。
- 前端事件闭环页面接入独立 API 数据切片、异步状态更新、菜单/按钮权限和服务端持久化提示。

验证记录：

- Prisma schema validate、前后端 TypeScript、阶段文件 ESLint：通过。
- Jest：应急事件服务、通用审批流和前端原型工作流共 17 项测试通过。
- 前端生产构建：通过。
- 运行时联调：4 条种子事件查询、操作员迁移 403、区域越权 404、研判启动、关闭申请、催办、QHSE 异人签署关闭及重复告警 409 均符合预期。

## 2026-07-15 · Stage 5B1 应急预案与演练后端化

计划提交：`build emergency plan workflow`

完成内容：

- 新增应急预案 Prisma 模型、3 套种子预案及内存/Prisma 双仓储。
- 新增预案查询、草稿创建/维护、提交评审、会签发布和历史版本回滚接口。
- 复用通用审批流执行 QHSE/生产负责人异人双会签，发布生成不可变版本，回滚只生成独立草稿。
- 新增演练计划、开始演练和评分复盘接口，全部使用预案修订号进行乐观并发控制。
- 新增 `plan:read/edit/submit/approve/drill` 权限和写操作审计。
- 前端预案库接入独立 API 数据切片、异步更新、菜单/按钮权限，演练全流程改由服务端持久化。

验证记录：

- Prisma schema validate、前后端 TypeScript、前端阶段文件 ESLint：通过。
- Jest：预案服务、通用审批流和前端原型工作流共 17 项测试通过。
- 前端生产构建：通过。
- 运行时联调：3 套种子查询、无权限访问 403、历史回滚、双账号会签发布 `V3.3`、演练计划→待复盘→92 分完成全流程通过。

## 2026-07-15 · Stage 5B2 应急资源后端化

计划提交：`build emergency resource inventory`

完成内容：

- 新增应急资源 Prisma 模型、3 类种子资源及内存/Prisma 双仓储。
- 新增资源入库、批次补充、调拨、到位、归还和巡检接口，写操作统一使用修订号进行乐观并发控制。
- 调拨按有效期 FEFO 自动分配批次，排除过期库存并阻断维护中资源；归还同步恢复批次和汇总可用库存。
- 新增 `resource:read/manage/dispatch/inspect` 权限、可信操作人和统一写操作审计。
- 前端资源管理接入独立 API 数据切片、异步刷新及菜单/按钮权限，API 模式不再依赖驾驶舱资源缓存。

验证记录：

- Prisma schema validate、前后端 TypeScript、前端阶段文件 ESLint：通过。
- Jest：应急资源服务和前端资源工作流共 13 项测试通过。
- 前端生产构建和后端生产编译：通过。
- 运行时联调：3 类种子查询、操作员调拨 403、呼吸器按近效期批次 `4 + 1` 分配、到位、归还恢复 12 套库存、维护资源调拨 409、可信巡检人均符合预期。

## 2026-07-15 · Stage 5C 融合通信后端化

计划提交：`build communication delivery workflow`

完成内容：

- 新增通信事件 Prisma 模型、3 条种子事件及内存/Prisma 双仓储。
- 新增通信事件查询、逐级升级、送达/失败回执和人员确认接口。
- 服务端固化 0/2/3/5 分钟重呼、班长、负责人/调度升级链，确认后禁止继续升级。
- 失败回执自动生成重试任务并限制最多重试 2 次；确认人来自认证主体。
- 新增 `communication:read/send/confirm` 权限、统一写操作审计和乐观并发控制。
- 前端融合通信控制台接入独立 API 数据切片、异步更新、菜单/按钮权限，并修复原有数组图标缺少 `key` 的 ESLint 问题。

验证记录：

- Prisma schema validate、前后端 TypeScript、前端阶段文件 ESLint：通过。
- Jest：通信服务和前端原型升级工作流共 11 项测试通过。
- 前端生产构建和后端生产编译：通过。
- 运行时联调：3 条种子查询、操作员升级 403、电话重呼、失败回执生成第 2 次重试、送达回执、可信确认人 `王强`、确认后继续升级 409 均符合预期。

## 2026-07-15 · Stage 6A1 统一遥测后端接入

计划提交：`build telemetry ingestion pipeline`

完成内容：

- 新增遥测点位和样本 Prisma 模型、6 个 GDS/VOC/MES 种子点位及内存/Prisma 双仓储。
- 新增点位筛选、详情、历史样本和标准样本写入接口。
- 样本使用外部 `sampleId` 幂等落库，质量码驱动在线/故障状态，并按各数据源点位阈值派生报警状态。
- 有效样本自动进入预警规则执行器，复用表达式、窗口、抑制和作业票联动能力；重复样本不重复触发规则。
- 新增 `telemetry:read/ingest` 权限、区域数据范围和写入审计。

验证记录：

- Prisma schema validate、后端 TypeScript 和差异检查：通过。
- Jest：遥测服务和预警执行服务共 9 项测试通过。
- 运行时联调：操作员仅查询所属区域 2 个点位、跨区域查询 404、写入 403；调度写入 GDS 二级样本后点位升至 `level2` 并评估 3 条规则，重复写入保持版本 2 且历史仅 1 条。

## 2026-07-15 · Stage 6A2 监测页面 API 接入

计划提交：`connect monitoring pages to telemetry api`

完成内容：

- 新增前端遥测通用类型、API 服务及 GDS/VOC/MES 视图模型映射。
- 模型新增三个监测数据切片、统一加载状态和样本写入后的局部刷新。
- GDS、VOC、MES 页面脱离共享 dashboard 数据，在 API 模式直接读取服务端点位并保留 Mock 演示模式。
- 三个模拟入口在 API 模式写入真实标准样本并执行服务端预警规则，按 `telemetry:ingest` 控制按钮权限。
- 显式向 Umi 浏览器包注入 `REACT_APP_QHSE_DATA_MODE`，修复此前 API 模式环境变量未生效的问题。

验证记录：

- 前端 TypeScript、阶段文件 ESLint、7 项遥测前后端测试和 API 模式生产构建：通过。
- 浏览器联调：GDS 18→42 `%LEL` 并进入二级报警；VOC 56→66 `mg/m³` 并显示超限/效率下降；MES 0.82→1.35 MPa 并显示参数异常、GDS 二级证据和联合规则已触发；无运行时 `TypeError`。

## 2026-07-15 · Stage 6A3 MQTT/WebSocket 实时遥测传输

计划提交：`build realtime telemetry transport`

完成内容：

- 新增可选 MQTT QoS 1 适配器，解析 `qhse/telemetry/{source}/{pointId}` 标准主题并复用统一幂等、状态派生和预警执行服务；未配置 broker 时保持禁用且不影响启动。
- 新增 Socket.IO `/telemetry` 实时通道，握手校验现有 Bearer 会话与 `telemetry:read` 权限，并按角色区域范围及订阅数据源过滤事件。
- 使用服务端单调序号和有界事件缓冲支持 `afterSequence` 断线补传，前端自动重连、保存游标，并在服务端重启序号归零时自动重置旧游标。
- 新增乱序样本策略：样本仍进入历史，但不覆盖点位当前值、不重复执行规则；未来时钟偏差超过默认 60 秒时返回稳定错误。
- 新增集成状态接口，公开 MQTT 启用/连接状态、收发计数以及 WebSocket 最新/最早补传序号，不暴露 broker 密钥。
- GDS、VOC、MES 页面接入实时订阅并显示连接状态，外部样本到达后无需刷新即可更新点位。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：统一遥测、MQTT 解析、序号补传和前端游标/映射共 14 项测试通过。
- 运行时联调：岗位操作员仅收到所属区域 GDS 事件、跨区域 MES 被过滤；断线后从序号 2 补传到序号 3，集成状态接口返回一致序号。
- 浏览器联调：GDS 页面显示“实时通道：已连接”，外部 API 将 GDS-101 从 33 写为 29 后页面无需刷新即完成更新；未发现 WebSocket 运行错误。

## 2026-07-15 · Stage 6B 安全附件与证据存储

计划提交：`build secure evidence storage`

完成内容：

- 新增统一附件领域模型、内存/Prisma 双仓储及本地文件/S3 兼容对象存储适配器。
- 上传执行 20 MB 默认上限、MIME 白名单、区域数据范围校验和 SHA-256 计算，兼容 multipart 中文文件名。
- 对象元数据与二进制内容分离；对象只允许绑定一条业务记录，同一绑定幂等，避免跨业务复用证据。
- 新增附件上传、元数据查询和鉴权下载接口，二进制响应绕过统一 JSON 包装并设置安全下载头。
- 隐患整改和应急事件证据接入真实对象绑定，证据记录固化对象 ID、类型、大小和可信哈希。
- 新增 `attachment:read/upload` 权限，前端 API 模式支持文件选择、上传、大小/哈希展示和鉴权下载。

验证记录：

- Prisma schema validate、前后端 TypeScript、后端生产编译及差异检查：通过。
- Jest：附件、隐患和应急事件领域共 15 项测试通过。
- 运行时联调：岗位操作员跨区域上传返回 403；所属区域 DOCX 上传、隐患绑定和下载成功，上传/下载/源文件 SHA-256 完全一致。

## 2026-07-15 · Stage 7A 统计报表中心

计划提交：`build qhse reporting center`

完成内容：

- 新增报表领域模块，复用隐患、预警、作业许可和应急事件服务统一计算周期 KPI，不复制业务数据。
- 新增日期范围、区域筛选、逐日趋势和区域绩效明细，统计周期限制为最多 366 天。
- 报表层对四类数据执行统一的区域数据范围二次裁剪，避免无区域参数的信号仓储泄露跨区域计数。
- 定义区域风险指数口径并明确仅用于报表排序，不替代风险分级结果。
- 新增 UTF-8 BOM CSV 导出、字段转义和导出审计，以及 `report:read/export` 分权权限。
- 新增统计报表前端页面，支持日期/区域筛选、四类指标卡、近 30 天组合趋势、区域排行和 CSV 下载。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：报表聚合、区域隔离、日期/数据量边界和 CSV 转义共 5 项测试通过。
- 运行时联调：企业领导可见 5 个有数据区域，装置负责人仅见 `area-02`；岗位操作员读取返回 403，装置负责人跨区域筛选返回 404；CSV 返回 200 且 BOM 为 `EF BB BF`。

## 2026-07-15 · Stage 7B 平台基础配置

计划提交：`build platform configuration center`

完成内容：

- 新增数据字典与集成登记 Prisma 模型、种子、内存/Prisma 双仓储和独立领域服务。
- 数据字典支持创建、排序项维护、启停和版本化更新，编码创建后不可修改，字典项值禁止重复。
- 集成登记支持遥测、通信、统一身份和对象存储四类接口的协议、地址、超时、责任方与启停维护。
- 集成地址强制声明协议一致，拒绝 URL 内嵌用户名/密码；Token、证书和密钥不写入业务数据库。
- 新增 `config:read/manage` 分权权限、写操作审计和乐观并发控制。
- 新增前端平台配置页，提供字典项动态编辑、集成登记维护和敏感信息边界提示。

验证记录：

- Prisma schema validate、前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：字典规范化/冲突/并发和集成地址安全边界共 5 项测试通过。
- 运行时联调：QHSE 可读取 2 套种子字典但写入返回 403，岗位操作员读取返回 403；管理员创建字典并更新到版本 2，旧版本覆盖返回 409，含账号密码的集成地址返回 400。

## 2026-07-15 · Stage 7C 运行诊断与可观测性

计划提交：`build runtime diagnostics`

完成内容：

- 新增进程内请求指标采集，按 HTTP 方法和控制器路由模板聚合请求量、错误量、平均/最大延迟及最后状态。
- 动态资源路径使用 `:id` 等路由模板归并，未匹配路径执行 UUID/数字段归一化，避免高基数指标持续增长。
- 新增受 `monitor:read` 保护的运行诊断接口，统一输出服务运行时、仓储/对象存储模式、内存和平台集成健康摘要。
- 诊断结果不返回集成地址、Token、证书或密钥，只公开运维排障所需的非敏感状态元数据。
- 新增前端运行诊断页，展示服务、存储、请求、内存和集成状态，并每 15 秒自动刷新。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：路由聚合、统计排序和敏感字段边界共 3 项测试通过。
- 运行时联调：管理员读取返回 200，岗位操作员返回 403，不存在风险返回 404 并聚合到 `/api/v1/risks/:id`；集成摘要未暴露连接地址。

## 2026-07-15 · Stage 7D API 安全基线

计划提交：`harden api authentication`

完成内容：

- 新增登录失败限流，按客户端 IP 与规范化账号组合维护 15 分钟窗口，第 5 次失败后返回稳定的 `LOGIN_RATE_LIMITED` 错误。
- 登录成功清除对应失败窗口；不同客户端和账号互不影响，避免一个账号的失败记录阻断其他用户。
- 单账号最多保留 5 个活动会话，创建新会话时清理过期会话并淘汰最早活动会话。
- 全局设置内容嗅探、页面嵌入、引用来源和浏览器能力限制响应头，HTTPS 请求额外设置 HSTS。
- 生产环境默认关闭 Swagger，仅在显式设置 `QHSE_SWAGGER_ENABLED=true` 时开放。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：认证会话和登录失败窗口共 8 项测试通过。
- 运行时联调：同一客户端前 5 次错误密码返回 401，第 6 次返回 429 和 `LOGIN_RATE_LIMITED`；健康接口包含 4 类安全响应头。

## 2026-07-15 · Stage 7E 可切换缓存基础设施

计划提交：`build resilient report cache`

完成内容：

- 新增统一缓存服务、内存适配器和 Redis 适配器；本地默认内存模式，生产通过运行环境切换 Redis。
- 缓存值统一 JSON 序列化，读取返回隔离副本；TTL 到期自动回源，避免共享对象被调用方意外修改。
- 同一缓存键的并发未命中请求合并为单次加载，防止高成本报表聚合发生缓存击穿。
- Redis 连接或读写失败时进入 30 秒降级窗口并直接回源，缓存故障不阻断报表接口。
- 统计报表摘要按日期、区域参数和认证主体授权区域隔离缓存 30 秒；CSV 导出保持实时计算。
- 运行诊断接口和前端诊断页新增缓存后端、状态、命中、未命中、写入和失败统计。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：缓存副本隔离、TTL、并发合并、故障回源、报表和诊断共 12 项测试通过。
- 运行时联调：内存模式连续查询得到 `hits=1/misses=1/writes=1`；Redis 不可用时报表仍返回 200，诊断显示 `backend=redis/status=degraded/failures=1`。

## 2026-07-15 · Stage 7F 持久化异步报表队列

计划提交：`build async report export queue`

完成内容：

- 新增进程内/BullMQ 双队列适配模式，本地无需中间件即可运行，生产通过 Redis 保存任务状态和结果。
- 新增后台报表导出创建、状态查询和鉴权下载接口，任务固化创建人、查询参数和区域数据权限快照。
- 只有任务创建人能够查询和下载结果，跨账号统一返回 404；无 `report:export` 权限的岗位操作员返回 403。
- Worker 并发数限制为 2，失败按指数退避最多重试 3 次；任务结果保留 24 小时并限制保留数量。
- Redis 生产者关闭离线命令排队和无限重连，队列不可用时快速返回 `503/REPORT_QUEUE_UNAVAILABLE`。
- 统计报表前端改为创建后台任务、轮询状态并在完成后自动下载；运行诊断页显示当前任务队列后端。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：队列完成、未完成拦截、创建人隔离、重试、失败脱敏、报表和诊断共 11 项测试通过。
- 运行时联调：任务从 queued 转为 completed，CSV 下载返回 200 且 BOM 为 `EF BB BF`；跨账号查询 404、操作员创建 403；Redis 不可用时 5 ms 内返回稳定 503。

## 2026-07-15 · Stage 7G 结构化访问日志

计划提交：`build structured access logging`

完成内容：

- 新增全局结构化日志服务，按单行 JSON 输出稳定时间、级别、事件名和显式白名单字段。
- 新增访问完成中间件，记录请求 ID、HTTP 方法、控制器路由模板、状态码、耗时和已认证用户 ID。
- 动态资源 ID 使用与请求指标相同的路由归一化函数，避免日志聚合被高基数路径拆散。
- 日志不读取请求体、查询参数、Authorization 或 Cookie；401 请求不会记录无效 Token。
- 未处理异常不再直接打印异常对象，只记录异常类型；客户端继续收到不含内部细节的通用 500 响应。
- 运行诊断接口和前端页面展示访问日志 `json/disabled` 状态，支持通过 `QHSE_ACCESS_LOG=false` 关闭本地访问日志。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：访问日志字段/脱敏、内部异常脱敏和诊断汇总共 5 项测试通过。
- 运行时联调：200/404/401 分别输出 info/warn/warn JSON 事件，资源路径归并为 `/api/v1/risks/:id`；日志中未出现请求携带的 Token、password 或查询参数。

## 2026-07-15 · Stage 7H 分布式会话存储

计划提交：`build distributed session storage`

完成内容：

- 新增统一会话存储服务、进程内适配器和 Redis 适配器，本地默认内存，生产通过运行环境切换。
- Redis 会话使用 TTL 键和用户会话有序集合，原子写入并淘汰同账号最早会话，最多保留 5 个活动会话。
- AuthService 登录、认证和退出全部异步化；HTTP Guard、标准/兼容控制器及遥测 WebSocket 握手同步完成调用链改造。
- 会话存储不可用统一返回 `503/SESSION_STORE_UNAVAILABLE`，不误报密码或会话失效，也不静默回落本地存储。
- 安全审计区分 `session_invalid` 与 `session_store_unavailable`；WebSocket 返回独立的 `SESSION_STORE_UNAVAILABLE` 错误码。
- 运行诊断接口和前端诊断页显示会话后端、健康状态、操作量和失败量。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：共享存储语义、五会话上限、过期/删除、会话后端故障、认证和诊断共 12 项测试通过。
- 运行时联调：内存模式登录、`/auth/me`、退出后 401 全部通过，遥测 WebSocket 异步认证收到 `telemetry:ready`；Redis 不可用时登录 45 ms 内返回稳定 503。

## 2026-07-15 · Stage 7I 容器存活与就绪探针

计划提交：`build dependency readiness probes`

完成内容：

- 保留兼容健康接口，新增独立 `/api/health/live` 和 `/api/health/ready` 公共探针。
- liveness 仅检查 API 进程可响应，不访问外部依赖，避免依赖波动触发无意义进程重启。
- readiness 并行主动检查当前启用的 PostgreSQL、缓存、会话和任务队列，返回后端类型、状态和耗时。
- 单依赖检查默认 1500 ms 超时，可通过运行环境在 100–10000 ms 范围调整；超时或失败统一返回 `503/SERVICE_NOT_READY`。
- 缓存、会话和队列适配器增加主动 ping/check 能力，健康成功或失败同步更新各自诊断状态。
- 错误响应只公开依赖名称、后端、状态和耗时，不返回连接地址、账号、密码或底层异常消息。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：全依赖就绪、单依赖失败、检查超时及缓存/会话/队列回归共 13 项测试通过。
- 运行时联调：内存模式 legacy/live/ready 均返回 200，四项依赖全部 ready；Redis 缓存不可用时 liveness 保持 200，readiness 在 5 ms 内返回结构化 503，并只标记 cache 为 not_ready。

## 2026-07-15 · Stage 7J W3C 追踪上下文

计划提交：`build trace context propagation`

完成内容：

- 请求上下文解析 W3C `traceparent`，继承有效上游 `traceId`、父 span 和采样标记，并为每个入站 HTTP 请求创建新的服务端 `spanId`。
- 拒绝全零 trace/span、非法格式和不支持版本；无可信上游时使用密码学随机数创建根追踪上下文。
- 响应头返回当前服务端 `traceparent`，统一成功与异常响应新增 `traceId`，保留原有 `requestId` 兼容能力。
- 访问完成和未处理异常日志新增 `traceId`、`spanId`，存在有效上游时记录 `parentSpanId`，不扩大敏感数据采集范围。
- 本阶段只建设标准上下文生成、传播和日志关联，不将其描述为完整 OpenTelemetry span 导出能力。

验证记录：

- 前后端 TypeScript、前后端生产编译、阶段文件 ESLint：通过。
- Jest：有效父上下文传播、非法/全零上下文重建、访问日志字段与异常响应脱敏共 7 项测试通过；全量后端相关测试通过。
- 运行时联调：有效父 `traceId` 在响应头、响应体和 JSON 日志保持一致且服务端 `spanId` 已更新；非法全零头生成新上下文，404 异常响应仍可按 `traceId` 关联。
- 已知基线：全仓后端 ESLint 存在 124 个历史错误；前端登录页存在 1 个既有快照差异，本阶段未修改对应文件或快照。

## 2026-07-15 · Stage 7K OpenTelemetry 追踪导出

计划提交：`build opentelemetry trace export`

完成内容：

- 引入 OpenTelemetry API、Node Trace SDK、HTTP 语义约定和 OTLP/HTTP Exporter 的最小官方依赖集。
- 入站 HTTP 请求创建真实 `SERVER` span，远程父上下文参与采样并传播 `tracestate`，SDK 负责生成当前服务端 span ID。
- span 在响应 `finish` 或连接 `close` 时只结束一次，结束前以控制器路由模板更新名称，记录方法、路由和状态码，5xx 标记错误。
- 未配置 Collector 时保留上下文和 span 生命周期但不发起网络请求；配置标准 OTLP 端点后使用批处理导出，并在应用关闭时刷新。
- OTLP 端点只接受不内嵌凭据的 HTTP(S) URL；诊断接口和前端只展示导出模式、span 数量和最近结束时间，不暴露端点或 Header。
- 可观测性后端未加入 readiness 依赖，Collector 故障不会阻断 QHSE 核心业务接流。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：28 个后端测试套件、110 项测试全部通过，包括远程父上下文、`tracestate`、路由模板、5xx 状态、不安全端点拒绝和诊断汇总。
- 运行时联调：未配置端点时 API 正常启动且诊断显示 `exporter=disabled`；配置本地 Collector 后向 `/v1/traces` 发出 `application/json` 批量请求，载荷 1261 bytes 且包含 `qhse-api` 资源标识。
- 依赖审计沿用当前仓库基线 127 项告警（14 low、48 moderate、59 high、6 critical），未执行可能引入破坏性升级的自动修复。

## 2026-07-15 · Stage 7L 可重复容量基线

计划提交：`build repeatable capacity checks`

完成内容：

- 新增后端 HTTP 容量检查工具和 `server:capacity` 命令，无需额外压测依赖即可在 Node 运行环境执行。
- 支持配置目标、总请求数、并发数、单请求超时、P95 上限和最大错误率；默认仅请求公共 liveness。
- 输出总耗时、每秒吞吐、成功/错误/网络错误、状态码分布及 P50/P95/P99/最大延迟。
- P95 或错误率越界时返回非零退出码，可直接用于本地回归或后续流水线门禁；HTTP 4xx/5xx 与网络/超时错误分开计数。
- 目标 URL 只允许不内嵌凭据的 HTTP(S)，报告移除查询参数；可选 Bearer Token 只从临时运行环境读取且不会输出。
- 文档明确开发机结果不是生产容量结论，生产验证仍需覆盖目标资源规格、真实依赖和业务读写混合场景。

验证记录：

- 前后端 TypeScript、后端生产编译和阶段文件 ESLint：通过。
- Jest：分位数、HTTP/网络错误分类、超时、阈值判定和不安全配置拒绝共 6 项新增测试通过；后端全量 29 个套件、116 项测试通过。
- 运行时基线：本机内存模式、关闭访问日志、未启用 OTLP 导出时，以 50 并发完成 2000 次 liveness 请求，2000/2000 成功；吞吐 8184.33 req/s，P50 5.3 ms、P95 9.96 ms、P99 22.67 ms、最大 32.54 ms。
- 该结果仅证明工具、请求链和阈值机制可用，不代表生产环境 SLA 或业务接口容量。

## 2026-07-15 · Stage 7M PostgreSQL 备份完整性链

计划提交：`build database backup verification`

完成内容：

- 新增 `server:backup`，使用 PostgreSQL custom archive 生成带时间和随机后缀的自包含数据库备份。
- `DATABASE_URL` 在进程内拆分为 libpq 子进程环境，密码不进入 `pg_dump` 参数、命令输出或备份清单；原始连接串不传给子进程。
- 归档生成后使用 `pg_restore --list` 验证结构，设置文件 0600 权限，再写入包含文件名、数据库名、长度、SHA-256 和创建时间的版本化清单。
- 新增 `server:backup:verify`，离线校验清单结构、文件名、长度、哈希和归档目录，不连接或修改任何数据库。
- 工具不自动执行恢复、不删除旧备份；创建失败可能留下无清单的未完成文件，只有有效清单存在且复核通过的归档才视为候选可恢复备份。
- 文档明确真实恢复、业务一致性、RPO/RTO 和保留策略仍需在获授权的隔离环境完成。

验证记录：

- 后端生产编译和阶段文件 ESLint/Prettier：通过。
- Jest：凭据传递边界、归档/清单生成、离线复核、损坏拒绝和无效 PostgreSQL 地址共 6 项新增测试通过；后端全量 30 个套件、122 项测试通过。
- CLI 安全失败：缺少 `DATABASE_URL` 或 `QHSE_BACKUP_FILE` 时均返回非零退出码和稳定消息，不创建文件。
- 本机已检测到 PostgreSQL 14.18 的 `pg_dump` 与 `pg_restore`；当前无获授权测试数据库，因此未伪造真实备份或恢复成功记录。

## 2026-07-15 · Stage 7N 驾驶舱服务端读模型

计划提交：`build operational dashboard read model`

完成内容：

- 新增 `GET /api/qhse/dashboard`，从风险、GDS/VOC/MES、活动预警、应急事件、应急资源和融合通信服务聚合展示快照，替代默认 API 模式下不存在的接口。
- 快照按账号数据范围过滤区域、点位、风险、预警、应急事件和通信任务；缓存键包含排序后的区域范围，避免不同权限账号共享聚合结果。
- 服务端计算综合风险、在线率、活动告警、VOC 达标率、MES 异常和通信送达率，趋势在没有历史桶数据时保持当前值平线，不虚构波动。
- 默认 API 模式不再读取浏览器中的演示场景缓存；本地演示模式仍保留既有场景能力。
- 展示大屏每 30 秒在页面可见时刷新，避免重复并发请求；连续 90 秒未成功刷新时显示数据陈旧，接口失败时保留已有快照并明确显示异常。
- 移除没有监控依据的固定“接口健康度 99.8%”和“实时”文案，改为基于最近刷新结果的可验证状态。

验证记录：

- 后端生产编译、前端 TypeScript 和生产构建、后端新增文件 ESLint/Prettier：通过。
- Jest：新增区域权限、指标聚合和无活动应急事件待命视图测试；后端全量 31 个套件、124 项测试通过。
- 运行时联调：管理员读取 6 个区域及 6 个遥测点；`unit_manager` 只读取 `area-02` 和 2 个 GDS 点，两个账号均返回 200 且响应包含有效追踪标识。
- 已知基线：大屏文件仍有 1 个既有未使用图标导入和 5 个既有 JSX 数组 key 规则错误，本阶段未改动对应既有代码。

## 2026-07-15 · Stage 7O 预警信号处置闭环

计划提交：`build warning signal resolution workflow`

完成内容：

- 预警信号增加 `active → acknowledged → processing → closed` 服务端状态机、操作轨迹、更新时间和版本号，禁止跳过确认直接关闭。
- 新增信号详情、确认、开始处置和关闭接口；关闭必须填写处置结论，所有写操作执行权限、审计和乐观锁。
- 信号查询在分页限制前按账号区域范围过滤，详情和写操作再次校验区域，避免越权读取或处置其他装置预警。
- 新增 `warning:handle` 和 `warning:close` 权限：岗位操作员可确认和开始处置但不能关闭，装置负责人及授权管理角色可完成闭环。
- Prisma 信号模型增加操作轨迹和版本字段，内存/PostgreSQL 双仓储保持同一并发与状态语义；未关闭信号在抑制窗口内不会重复生成。
- API 模式的预警详情页改用真实确认、处置和关闭接口，关闭弹窗要求填写结论；服务端返回结果即时更新页面，后台刷新不再把状态恢复成待确认。
- 驾驶舱按信号状态展示待确认、已确认和处置中，关闭信号不再计入活动告警。

验证记录：

- 后端生产编译、前端 TypeScript 和生产构建通过；本阶段新增/独立文件及前端增量 ESLint 通过。
- Jest：新增区域隔离、完整状态迁移、禁止跳态、旧版本冲突和角色权限测试；后端全量 31 个套件、126 项测试通过。
- 运行时联调：QHSE 账号生成 `area-02` 信号，`environment` 查询结果为 0；岗位操作员关闭返回 403，装置负责人确认/处置/关闭依次返回 200，旧版本处置返回 409，最终版本为 4 且包含 3 条操作记录。
- 关闭后装置负责人驾驶舱只返回 `area-02` 且活动告警为 0；错误与访问日志使用路由模板并保留追踪字段。
- 已知基线：既有预警执行服务和 Prisma 仓储仍有 12 个函数声明顺序 ESLint 规则错误，本阶段未重排既有函数结构。

## 2026-07-15 · Stage 7P 预警证据核验持久化

计划提交：`build warning evidence verification`

完成内容：

- 预警信号增加监测数据、工艺参数、作业票证和关联人员四类证据核验记录，保存核验人 ID、姓名和服务端时间。
- 新增 `POST /api/v1/warning-execution/signals/:id/evidence`，使用白名单 DTO 拒绝未知类别，并执行 `warning:handle` 权限、区域范围和操作审计。
- 同一信号的同类证据只能核验一次，重复操作返回稳定 409；已关闭信号禁止追加核验，写入继续使用预警信号版本号防止并发覆盖。
- 内存与 Prisma 仓储同步保存核验记录和对应时间线操作，Prisma schema 增加默认空数组字段，兼容新建信号。
- 驾驶舱读模型将服务端核验记录映射到预警详情，API 模式核验按钮调用真实接口并即时更新进度、核验人、时间线和版本。
- 没有 `warning:handle` 权限时核验按钮禁用并显示明确原因；核验请求进行中时阻止重复点击。

验证记录：

- 后端生产编译、前端 TypeScript、前端生产构建、Prisma Client 生成和 schema 校验通过；阶段增量 ESLint 通过。
- Jest：新增可信核验人、版本递增、重复阻断及驾驶舱映射测试；后端全量 31 个套件、127 项测试通过。
- 运行时联调：`unit_manager` 首次核验返回 200、版本升至 2，重复核验返回 409；`environment` 跨区域核验返回 404，非法类别返回 400。
- 驾驶舱返回核验类别、核验人、服务端时间和“证据核验”时间线操作，与信号版本保持一致。

## 2026-07-15 · Stage 7Q 预警到应急事件联动

计划提交：`build warning emergency linkage`

完成内容：

- 新增 `POST /api/v1/warning-execution/signals/:id/emergency`，从已确认预警生成待研判应急事件，并把预警推进到处置中。
- 联动入口同时要求 `warning:handle` 与 `emergency:manage`，服务层再次执行区域数据范围校验；无管理权限返回 403，跨区域资源保持 404 语义。
- 使用预警信号 ID 作为应急事件来源 ID，创建前查询、重复调用复用已有事件；若事件创建后信号发生版本冲突，刷新版本重试可继续完成状态推进，不重复创建事件。
- 预警等级映射为应急响应等级：重大预警映射 II 级、较大预警映射 III 级，其余映射 IV 级；事件摘要固化预警详情和已核验证据数量。
- API 模式预警详情改用真实联动接口，按钮同时遵循两项权限；成功后即时更新预警时间线、大屏活动告警和应急事件独立数据源。
- 保留独立“开始处置”接口供非应急处置场景使用，避免改变既有客户端和状态机语义。

验证记录：

- 后端生产编译、前端 TypeScript 和生产构建通过；后端全量 31 个套件、128 项测试通过。
- 新增服务测试覆盖等级映射、事件生成、信号状态推进、操作轨迹和重复调用幂等性。
- 运行时联调：岗位操作员启动应急返回 403，跨区域环境管理员返回 404，装置负责人首次及重复启动均返回 200，事件数量保持 1。
- 大屏读模型同步显示预警“处置中”和新建待研判事件；重大 GDS 预警映射为 II 级响应。
- 已知基线：应急事件服务既有 `nextState` 声明顺序 ESLint 错误未在本阶段重排；Jest 仍提示既有异步句柄未退出，但全部测试正常完成并返回 0。

## 2026-07-15 · Stage 7R 风险分级前端 API 闭环

计划提交：`connect risk management APIs`

完成内容：

- 新增风险 API 客户端，API 模式的 LEC 评估和管控措施维护不再只修改浏览器内存，直接调用既有风险领域接口并使用当前风险版本防止覆盖。
- 评估请求只提交 L/E/C、评估依据和版本，不发送表单中的评估人；服务端继续从认证会话固化评估人 ID 与姓名，阻止前端伪造审计身份。
- 风险页等待服务端成功响应后再关闭弹窗并提示，提交期间阻止重复保存；页面用服务端返回的风险单元即时替换驾驶舱记录，显示真实评估历史、措施和新版本。
- 缓存存储增加按前缀删除能力，内存和 Redis 适配器保持同一语义；风险写入成功后失效全部授权范围的驾驶舱缓存，刷新页面不会在 TTL 内恢复旧数据。
- 命名空间失效会先等待同命名空间在途加载完成再删除，避免并发旧快照在失效后重新写回；缓存失效失败只记录降级，不回滚已经成功的业务写入。

验证记录：

- 前后端 TypeScript、后端生产编译、前端生产构建和阶段增量 ESLint 通过。
- 新增请求契约与缓存失效测试；后端及风险客户端全量 32 个套件、131 项测试通过。
- 运行时联调：装置负责人评估返回 201、措施维护返回 200，版本从 1 递增到 3；响应评估人为登录账号“李建国”，旧版本覆盖返回 409，跨区域读取返回 404。
- 在写入前先填充驾驶舱缓存，评估和措施写入后立即重新读取均返回最新版本，且两类操作均可在审计日志中查询。
- 已知基线：Jest 仍提示既有异步句柄未退出，但全部测试正常完成并返回 0。

## 2026-07-18 · Stage 7S 作业许可实时预警联动前端闭环

计划提交：`connect work permit warning linkage`

完成内容：

- 新增作业许可联动 API 快照，前端并行读取真实票证、授权风险区域、GDS 点位、预警规则和活动信号，API 模式不再强制清空点位与告警。
- 作业许可页面根据已发布且启用的联动规则显示服务端运行状态；“刷新联动结果”只同步服务端权威数据，不在浏览器重复执行历史信号。
- 前端监测模拟写入成功后立即同步票证、规则和信号，返回作业许可页面可直接看到“建议暂停”；申请表恢复同区域 GDS 探测器选择和距离提示。
- 遥测服务把 GDS、VOC、MES 原始主指标转换为规则表达式使用的标准指标，并携带 VOC 限值、MES 上下限；表达式阈值支持引用同一样本中的动态限值。
- GDS API 模拟值调整为 55%LEL，与当前已发布重大预警规则一致；规则触发后由服务端自动调用暂停建议领域接口。
- 将规则执行辅助函数移到使用位置之前，清除该服务既有的 6 个声明顺序 ESLint 错误，不改变规则状态机语义。
- 同步更新系统业务操作手册，移除“API 模式只能使用 Mock 演示联动”的过时限制。

验证记录：

- 阶段 4 个测试套件、20 项测试通过；新增 API 快照聚合、联动摘要、遥测指标标准化和动态阈值引用测试。
- 前端 TypeScript、后端生产编译、前端生产构建和阶段文件 ESLint 通过。
- 运行时联调：`permit-001` 初始为“作业中/V1”；写入 GDS-101 55%LEL 后生成重大预警 `GDS_L2_01`，返回 `linkedPermitIds: [permit-001]`，票证更新为“建议暂停/V2”并保留规则原因。
- 全量 Jest 共 48 个套件、201 项测试，其中 47 个套件、200 项通过；唯一失败仍是未改动登录页的既有快照差异。Jest 仍提示既有异步句柄未退出。

## 2026-07-18 · Stage 7T GDS/MES 跨点位联合预警

计划提交：`connect cross-source joint warning`

完成内容：

- 修正规则执行状态只按原始点位隔离的问题：联合规则改为使用 `area:<areaId>` 作为研判对象，同一区域不同 GDS/MES 点位可共享指标和采样时间。
- 联合规则的灰度分群、抑制查询、状态持久化和信号对象统一使用区域研判对象，生成信号的数据来源明确标记为“联合预警”。
- GDS 采样根据点位前值自动派生 `up/down/stable` 趋势，联合规则无需客户端伪造 `GDS.trend`。
- 新增催化裂化装置 MES 压力点 `mes-pt-201`，MES 页面模拟入口改为写入同区域 MES 与 GDS 样本。
- API 模式从真实预警信号数据源读取联合预警，模拟成功后可直接跳转预警详情；Mock 模式继续读取演示驾驶舱事件。
- 更新功能缺口清单和系统业务操作手册，明确联合预警的区域关联、趋势派生和演示步骤。

验证记录：

- 定向 2 个测试套件、18 项测试全部通过，新增测试先复现跨点位无法联合触发，再验证区域聚合、联合信号来源和 GDS 趋势派生。
- 前后端 TypeScript、后端生产编译、前端生产构建、阶段文件 ESLint 和后端阶段文件 Prettier 检查通过。
- 运行时联调：先写入 `mes-pt-201` 压力 1.35 MPa 未单独触发，再写入同区域 `gds-101` 浓度 42%LEL，服务端生成重大联合信号 `rule-003`，研判对象为 `area:area-02`、来源为“联合预警”。
- 全量 Jest 共 48 个套件、202 项测试，其中 47 个套件、201 项通过；唯一失败仍是未改动登录页的既有快照差异。

## 2026-07-18 · Stage 7U 事件调查复盘服务端闭环

计划提交：`build event review workflow`

完成内容：

- 新增独立事件复盘模块、内存/Prisma 双仓储、数据库模型和种子，API 模式不再依赖驾驶舱中的空 `eventReviews` 数组。
- 提供区域复盘台账和详情接口，按认证账号数据范围过滤；跨区域读取保持资源不存在语义。
- 整改措施按“待整改 → 整改中 → 已完成”推进，服务端固化操作人 ID、姓名和时间，已完成措施禁止重复推进。
- 所有整改完成后才允许关闭归档；关闭接口要求 `emergency:approve`，普通处置使用 `emergency:manage`，两类写入均记录统一审计。
- 内存和 Prisma 仓储使用相同版本语义，旧页面写入返回稳定 `VERSION_CONFLICT`，避免并发覆盖整改状态。
- 前端复盘页改用独立 API 数据切片，按按钮权限控制推进和归档；Mock 模式继续保留原有演示交互。
- 更新路由权限、功能缺口、后端路线和系统业务操作手册，明确已完成边界及自动建档、调查报告编辑等后续项。

验证记录：

- 阶段 2 个测试套件、6 项测试全部通过，覆盖区域隔离、状态迁移、可信操作人、归档门槛、版本冲突和 API 请求契约。
- 前后端 TypeScript、后端生产编译、Prisma Client 生成与 schema 校验、前端生产构建、阶段文件 ESLint/Prettier 检查通过。
- 运行时联调：`environment` 查询 0 条、`unit_manager` 查询 1 条；整改推进后版本升至 2 并记录“李建国”，旧版本返回 409，未完成归档返回 400；全部措施完成后 `qhse` 归档成功，状态为“已复盘”、版本为 7、复盘人为“赵磊”。
- 全量 Jest 共 50 个套件、208 项测试，其中 49 个套件、207 项通过；唯一失败仍是未改动登录页的既有快照差异。Jest 仍提示既有异步句柄未退出。

## 2026-07-18 · Stage 7V 事件关闭自动复盘建档

计划提交：`link event closure to review`

完成内容：

- 应急事件异人审批关闭成功后，由服务端自动创建对应复盘档案，不再依赖预置种子或人工补录台账。
- 复盘档案使用应急事件 ID 做唯一关联，内存与 Prisma 仓储均提供幂等创建；关闭后重复请求可补偿缺失档案且不会生成重复记录。
- 自动档案继承事件编码、标题、区域、摘要、责任部门和事件操作时间线，并生成一项“完成事件调查报告和经验反馈”初始整改措施。
- 新增结构化调查结论更新接口，统一维护事件摘要、直接原因、根本原因和经验教训；复盘负责人由认证主体固化，客户端不能伪造。
- 归档门槛增加调查结论完整性校验，只有结论完整且所有整改措施已完成时才能归档。
- 前端复盘页增加调查结论编辑弹窗、空值提示和归档条件提示，API 模式按 `emergency:manage` 控制编辑、按 `emergency:approve` 控制归档。
- 调整应急事件服务辅助函数声明顺序，清除该阶段触及文件的既有 ESLint 声明顺序错误。

验证记录：

- 阶段 3 个测试套件、14 项测试全部通过，覆盖自动建档、重复补偿、调查更新、可信负责人、关闭联动和前端请求契约。
- 前后端 TypeScript、后端生产编译、前端生产构建、阶段文件 ESLint/Prettier 检查通过。
- 运行时联调：`dispatcher` 申请关闭 `lifecycle-003`，`qhse` 异人审批后事件状态为“已关闭/V3”，自动生成 `evt-003` 复盘档案；调查结论保存至 V2，系统整改项完成后成功归档为“已复盘/V5”。
- 全量 Jest 共 50 个套件、211 项测试，其中 49 个套件、210 项通过；唯一失败仍是未改动登录页的既有快照差异。

## 2026-07-18 · Stage 7W 复盘附件与整改计划维护

计划提交：`build review evidence and actions`

完成内容：

- 统一附件业务类型增加 `event_review`，复盘调查附件复用现有上传、内容类型/大小校验、对象存储、SHA-256 和区域权限链。
- 复盘模型增加调查证据集合，支持调查报告、现场照片、检测报告和培训记录；绑定时固化认证上传人、文件哈希、类型和大小。
- Prisma 模型、种子、内存仓储和 PostgreSQL 仓储同步持久化证据，复盘写入仍使用同一版本号防止并发覆盖。
- 新增整改措施创建和调整接口，支持标题、责任部门、责任人、完成期限和优先级；已完成措施及已归档复盘禁止继续调整。
- 前端复盘页增加调查附件上传/下载、整改措施新增和未完成措施调整入口，沿用附件与应急管理权限。
- 更新功能缺口、后端接口路线和业务操作手册，收敛剩余边界为隐患联动、知识库检索和复盘报告导出。

验证记录：

- 阶段 2 个测试套件、12 项测试全部通过，覆盖真实附件绑定、可信上传人/哈希、整改新增/调整及前端版本请求契约。
- 前后端 TypeScript、后端生产编译、Prisma Client 生成与 schema 校验、前端生产构建、阶段文件 ESLint/Prettier 检查通过。
- 运行时联调：上传 `README.md` 后生成 SHA-256，绑定 `review-001` 后版本升至 V2并记录“赵磊”；新增整改措施后升至 V3，调整为生产运行部/陈涛、期限 `2026-08-01`、紧急优先级后升至 V4。
- 全量 Jest 共 50 个套件、214 项测试，其中 49 个套件、213 项通过；唯一失败仍是未改动登录页的既有快照差异。

## 2026-07-18 · Stage 7X 复盘整改转隐患治理

计划提交：`link review actions to hazards`

完成内容：

- 新增复盘整改转隐患接口，继承措施标题、责任部门、责任人、期限和整改内容，来源固定为“复盘整改”。
- 转单要求同时具备应急管理和隐患上报权限，隐患风险单元必须与复盘事件所属区域一致，避免跨区域错误挂接。
- 复盘措施保存隐患 ID、编号、状态和关联时间；重复转单复用已关联隐患，不重复创建治理任务。
- 已关联措施禁止在复盘页继续调整或手工推进，统一由隐患治理状态机维护，避免两套状态相互冲突。
- 新增隐患状态同步接口，将待整改映射为待整改、整改中/待验收映射为整改中、已关闭映射为已完成，并保留乐观锁。
- 前端复盘页增加风险单元选择、隐患等级和类别表单、关联编号展示、隐患治理跳转和状态同步入口。
- 更新功能缺口、后端接口路线和系统业务操作手册，事件复盘剩余边界收敛为知识库检索和正式报告导出。

验证记录：

- 定向 3 个测试套件、20 项测试通过，覆盖转单字段映射、重复幂等、状态同步、同区域校验和前端请求契约。
- 前后端 TypeScript、后端生产编译、前端生产构建、阶段增量 ESLint 和后端阶段文件 Prettier 检查通过。
- 运行时联调：装置负责人将 `action-003` 转为隐患后复盘升至 V2；重复请求复用同一隐患；隐患开始整改后同步回复盘，措施状态更新为“整改中”、复盘升至 V3。
- 全量 Jest 共 50 个套件、216 项测试，其中 49 个套件、215 项通过；唯一失败仍是未改动登录页的既有快照差异。

## 2026-07-18 · Stage 7Y 事件复盘正式报告导出

计划提交：`export event review reports`

完成内容：

- 新增服务端事件复盘报告生成器，输出自包含、A4 打印优化的 UTF-8 HTML，不依赖浏览器拼装业务内容或外部静态资源。
- 报告覆盖事件与复盘元数据、事件摘要、直接原因、根本原因、经验教训、全过程时间线、整改责任、隐患关联、调查证据和 SHA-256 目录。
- 所有业务文本统一 HTML 转义，避免调查内容或附件名称注入可执行标记；未归档报告显示“待归档”水印。
- 新增带区域数据范围、`emergency:read` 与 `report:export` 双权限、下载审计的报告接口，并设置内容类型、长度、防嗅探和 UTF-8 文件名响应头。
- 前端复盘页增加“导出复盘报告”入口，使用服务端 Blob 下载；下载后的报告可直接浏览或打印为 PDF。
- 更新功能缺口、后端接口路线和业务操作手册，事件复盘剩余边界收敛为企业知识库检索。

验证记录：

- 定向 3 个测试套件、16 项测试通过，新增报告结构、中文文件名、业务文本转义和前端下载契约测试。
- 前后端 TypeScript、后端生产编译、前端生产构建、阶段增量 ESLint 和后端阶段文件 Prettier 检查通过。
- 运行时联调：`qhse` 下载 `review-001` 返回 200、`text/html; charset=utf-8`、5128 字节和 UTF-8 文件名；无 `report:export` 权限的 `unit_manager` 返回 403。
- 全量 Jest 共 51 个套件、218 项测试，其中 50 个套件、217 项通过；唯一失败仍是未改动登录页的既有快照差异。

## 2026-07-18 · Stage 7Z 企业知识库结构化检索

计划提交：`build cross-domain knowledge search`

完成内容：

- 新增独立知识库聚合模块，统一读取事件复盘、隐患治理和应急预案领域，不复制三类业务数据。
- 只纳入已归档复盘、已关闭隐患和已发布生效预案，阻止草稿、调查中事件或整改中隐患作为正式经验被复用。
- 支持关键词、知识类型和 1–20 条结果上限；编码、标题和正文采用不同权重，按相关度、更新时间和编码稳定排序。
- 连续中文短语自动生成二字检索片段，“法兰泄漏”等自然输入无需手工插入空格。
- 复盘和隐患查询继承账号区域数据范围，企业预案保持全局正式资产语义；接口同时要求复盘、隐患和预案三类读取权限。
- 复盘页新增“查相似案例”弹窗，展示知识类型、编码、标题、摘要、区域、状态、相关度和匹配内容。
- 更新功能缺口、后端接口路线和业务操作手册，明确结构化检索已完成及附件解析、OCR、全文/向量索引的后续生产化边界。

验证记录：

- 定向 2 个测试套件、3 项测试通过，覆盖正式知识过滤、跨域排序、类型/上限、区域参数传递和前端请求契约。
- 前后端 TypeScript、后端生产编译、前端生产构建、阶段增量 ESLint 和后端阶段文件 Prettier 检查通过。
- 运行时联调：`qhse` 搜索“压力表”返回已关闭隐患 `YH20260709009`；`unit_manager` 因区域范围返回 0 条；连续短语“法兰泄漏”命中 2 项已发布预案；缺少 `plan:read` 的 `environment` 返回 403。
- 全量 Jest 共 53 个套件、221 项测试，其中 52 个套件、220 项通过；唯一失败仍是未改动登录页的既有快照差异。

## 2026-07-18 · Stage 8A 登录与权限回归基线

计划提交：`stabilize login regression tests`

完成内容：

- 修正登录场景录制数据及其缓存源，为管理员账号补齐 `access: admin`，保证每次 Umi 生成测试文件后仍能稳定复现完整权限。
- 登录成功测试改为校验 `/dashboard` 跳转、应用身份以及风险分级、隐患治理、作业许可等受控菜单，不再依赖易受路由和组件结构影响的整页 DOM 快照。
- 保留登录表单快照，用于覆盖未登录页面结构；删除已经包含历史错误页和过期菜单的登录后快照。
- 新增权限工厂单元测试，覆盖兼容管理员、按权限精确授权和未登录拒绝三种边界。

验证记录：

- 登录与权限定向测试共 2 个套件、5 项测试全部通过。
- 前端 TypeScript、阶段文件 ESLint 和 Prettier 检查通过。
- 全量 Jest 共 54 个套件、224 项测试和 1 个快照全部通过，历史唯一失败基线已清除。

## 2026-07-18 · Stage 8B 组织与用户授权管理

计划提交：`build iam authorization management`

完成内容：

- 新增用户授权维护接口，支持账号启停、所属组织、角色组合和区域数据范围调整，并使用版本号阻止并发覆盖。
- 服务端校验组织、角色和区域真实性；区域角色必须至少分配一个区域，全企业角色自动清除冗余区域授权。
- 禁止管理员停用自身或移除自己的系统管理员角色，避免平台因误操作失去管理入口。
- 认证服务改为每次会话校验读取当前账号授权，角色和区域变更即时作用于已有会话，账号停用后原会话立即失效。
- 新增“组织与权限管理”页面，提供账号筛选、用户/角色/组织/区域指标、授权台账、启停和角色/区域维护。
- 新增 `iam:manage` 写权限、操作审计和前端 `canAdmin` 路由控制；更新功能缺口、后端路线和系统业务操作手册。
- 当前写入使用进程内 IAM 仓储，用户新增、自定义角色、PostgreSQL IAM 仓储、统一身份源和权限变更审批保留为后续生产化阶段。

验证记录：

- IAM、认证和前端请求契约 3 个专项套件、13 项测试通过；包含并发冲突、无效授权、区域门槛、自锁死保护和现有会话实时生效。
- 全量 Jest 共 56 个套件、231 项测试和 1 个快照全部通过。
- 前后端 TypeScript、后端生产编译、前端生产构建、阶段文件 ESLint 和 Prettier 检查通过。
- 运行时联调：管理员更新操作员授权后版本由 V1 升至 V2，原会话立即获得 `unit_manager` 角色；停用后原会话返回 401/`SESSION_INVALID`；管理员自停用返回 400/`IAM_SELF_LOCKOUT`。

## 2026-07-18 · Stage 8C IAM PostgreSQL 持久化

计划提交：`persist iam authorization changes`

完成内容：

- 抽取 IAM 仓储契约和版本化用户快照，提供内存与 Prisma 两种实现，沿用 `QHSE_REPOSITORY` 模式选择。
- IAM 服务启动时从所选仓储加载组织、区域、角色和用户关系，同时保持认证链同步读取当前授权的低延迟语义。
- Prisma 授权更新复用 `User.tokenVersion` 作为乐观锁修订号，不新增数据库迁移。
- 用户状态和组织更新、版本递增、角色关系替换及区域关系替换在同一数据库事务内完成，任一操作失败整体回滚。
- 写仓储成功后才替换服务内授权快照，避免数据库失败时出现内存与持久化状态分裂。
- 更新功能缺口、后端路线和系统业务操作手册，IAM 剩余生产化边界收敛为用户新增、自定义角色、统一身份源、权限变更审批和多实例授权变更广播。

验证记录：

- IAM 仓储、服务和认证 3 个专项套件、14 项测试通过，覆盖内存重载、Prisma 关系映射、事务关系替换、版本冲突和会话实时生效。
- 全量 Jest 共 57 个套件、234 项测试和 1 个快照全部通过。
- Prisma Client 生成、schema 校验、前后端 TypeScript、后端生产编译和前端生产构建通过；schema 复用现有模型，无需新增迁移。
- 内存模式 HTTP 联调更新 `user-operator` 后返回 200，重新查询确认版本升至 V2、组织切换为 `org-storage`、区域更新为 `area-05`。
- 当前环境未配置 `DATABASE_URL` 且 Docker daemon 未运行，因此本阶段未执行真实 PostgreSQL 进程联调；Prisma 事务路径由仓储契约测试覆盖，部署前仍需在目标数据库执行种子、授权更新和重启重载验证。
